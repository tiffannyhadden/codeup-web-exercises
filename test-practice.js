// /*1.) Create a function called lowestNumber that takes in  2 numbers and returns the lowest. Have it return false if it is not a number*/
// // function lowestNumber(number1, number2) {
// //     if(isNaN(number1) || isNaN(number2)) {
// //         return false
// //     } else {
// //         if(number1 < number2) {
// //             return number1;
// //         } else {
// //             return number2;
// //         }
// //     }
// // }
// // console.log(lowestNumber("five7","56twentythree"))
//
// /*2.) Create a function called bunnyEars that takes in a number of bunnies. Each bunny has two floppy bunny ears. Make your function return how many bunny ears there will be for the number given*/
// function bunnyEars(number) {
//     return number * 2;
// }
// /*3.) Create a function called hello that takes in a string. Given a string name, e.g. "Bob", return a greeting of the form "Hello Bob!". Make sure it is not a number or boolean*/
// // function hello(string) {
// //     if(isNaN(string)) {
// //         return ("Hello " + string + "!" )
// //     } else {
// //         return false;
// //     }
// // }
//
// // function hello(string) {
// //     if(typeof string === "string") {
// //         return ("Hello " + string + "!" )
// //     } else {
// //         return false;
// //     }
// // }
// // // if not a number try using typeof
//
// /*4.) Create a function called firstHalf that takes in a string. Given a string of even length, return the first half. So the string "WooHoo" yields "Woo".*/
// // function firstHalf(string) {
// //     if(string.length % 2 === 0) {
// //         return string.slice(0, string.length / 2);
// //     }
// // }
// // console.log(firstHalf("WooHoo"))
// /*5.) Create a function called middleThree that takes in a string. Given a string of odd length, return the string length 3 from its middle, so "Candy" yields "and". The string length will be at least 3.*/
// // function middleThree(string) {
// // if(string.length % 2 !== 0) {
// //
// //     let mid = (string.length + 1)/2;
// //     return string.slice(mid -2, mid + 1);
// //     }
// //     return string;
// // }
// // console.log(middleThree("candy"));
// /*6.) Create a function called twoChar that takes in a string. Given a string and an index, return a string length 2 starting at the given index. If the index is too big or too small to define a string length 2, use the first 2 chars. The string length will be at least 2.*/
// function twoChar(string) {
//     return
//
// }
// /*7.) Create a function called comboString that takes in 2 strings Given 2 strings, a and b, return a string of the form short+long+short, with the shorter string on the outside and the longer string on the inside. The strings will not be the same length, but they may be empty (length 0).*/
// function comboString() {
//
// }
// /*8.) Create a function called doubleSum that takes in 2 numbers Given two int values, return their sum. Unless the two values are the same, then return double their sum.*/
// function doubleSum(a, b) {
//     var result = a + b;
//     if( a, b = )
//     return result;
//
// }
//
// console.log(doubleSum(4, 4 ));
// /*9.) Create a function called division that takes in two numbers and divides them and tells you what remainder is*/
// // function division(num1, num2) {
// //     if(typeof num1 !== "number" || typeof num2 !== "number") {
// //         return false;
// //     } else {
// //         return parseFloat(num1) % parseFloat(num2);
// //     }
// // }
// //
// // function division(num1, num2) {
// //     if(isNaN(num1) || isNaN(num2)) {
// //         return false;
// //     } else {
// //         return parseFloat(num1) % parseFloat(num2);
// //     }
// // }
//
// /*10.)Creat a function called cigarParty that takes in a number and a boolean. When squirrels get together for a party, they like to have cigars. A squirrel party is successful when the number of cigars is between 40 and 60, inclusive. Unless it is the weekend, in which case there is no upper bound on the number of cigars. Return true if the party with the given values is successful, or false otherwise.*/
// function cigarParty(number, boolean) {
//
// }